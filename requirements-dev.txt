# Development and CI tooling. Runtime dependencies live in
# src/functions/requirements.txt, which is what gets deployed.
-r src/functions/requirements.txt

pytest>=8.3,<9
pytest-cov>=6.0,<8
ruff>=0.8,<1
pyyaml>=6,<7
